events {
    worker_connections 1024;
}

http {
    access_log /var/log/nginx/access.log;
    error_log /var/log/nginx/error.log;

    proxy_connect_timeout 10s;
    proxy_read_timeout    30s;
    proxy_send_timeout    30s;

    upstream user-service         { server user-service; }
    upstream product-catalog-service { server product-catalog-service; }
    upstream order-service        { server order-service; }
    upstream notification-service { server notification-service; }
    upstream payment-service      { server payment-service; }

    server {
        listen 80;
        server_name _;

        proxy_http_version 1.1;
        proxy_set_header Connection "";
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;

        error_page 502 503 504 = @upstream_error;
        location @upstream_error {
            default_type application/json;
            return 502 '{"status":"error","message":"Service temporarily unavailable"}';
        }

        # User Service
        location /users/      { proxy_pass http://user-service/users/; }
        location /auth/       { proxy_pass http://user-service/auth/; }
        location /health/user { proxy_pass http://user-service/health; }

        # Product Service
        location /products/      { proxy_pass http://product-catalog-service/products/; }
        location /health/product { proxy_pass http://product-catalog-service/health; }

        # Order Service
        location /orders/      { proxy_pass http://order-service/orders/; }
        location /health/order { proxy_pass http://order-service/health; }

        # Notification Service
        location /notifications/      { proxy_pass http://notification-service/notifications/; }
        location /health/notification { proxy_pass http://notification-service/health; }

        # Payment Service
        location /payments/      { proxy_pass http://payment-service/payments/; }
        location /health/payment { proxy_pass http://payment-service/health; }

        # Gateway Health
        location = /health {
            default_type application/json;
            return 200 '{"status":"online","service":"api-gateway"}';
        }
        location = /health/gateway {
            default_type application/json;
            return 200 '{"status":"online","service":"api-gateway"}';
        }
    }
}
